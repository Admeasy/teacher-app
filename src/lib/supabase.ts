// Re-export the single canonical Supabase client (env-driven, auto-generated).
// All app code should ultimately resolve to the same client + project.
export { supabase } from "@/integrations/supabase/client";
