# Legacy ERP pages removed

The original Vite/React Router screens lived here. They conflicted with Next.js Pages Router (`src/pages` is reserved).

Teacher UI now lives under:

- `src/app/` — App Router routes
- `src/teacher/pages/` — screen components
